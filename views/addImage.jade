html
    head
        title  ImageWall |
            != title
    body
        h1!= message

        form(action="...", method="post", enctype="multipart/form-data")
            input(type="file", name="displayImage")

    script(src="/socket.io/socket.io.js")
    script.
        var socket = io();
        $('form').submit(function(){
            socket.emit('form image', $('#m').val());
            return false;
        });
