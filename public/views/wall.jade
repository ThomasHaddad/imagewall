html
    head
        link(rel='stylesheet', href='/css/styles.css')
        script(src="/js/jquery.js")
        script(src="/js/jquery.scrollTo.min.js")
        script(src="/js/jquery-ui.min.js")
        title ImageWall |
            !=  title
    body
        div#imageWall
            - each image in images
                - var self = (image.owner == client)
                if(self)
                    div.image(data-client="#{image.owner}" data-self="true")
                        if(image.filteredUrl)
                            img(src=image.filteredUrl )
                        else
                            img(src=image.formatedUrl)
                        if(image.message)
                            p
                                !=image.message
                else
                    div.image(data-client="#{image.owner}")

                        if(image.filteredUrl)
                            img(src=image.filteredUrl )
                        else
                            img(src=image.formatedUrl)
                        if(image.message)
                            p
                                !=image.message
        footer
            a(href='add') Participer!
            a(id="locate" href="#") Se repérer
    script(src="/socket.io/socket.io.js")
    script(src="/js/parallax.js")
    script.

        var client= !{JSON.stringify(client)}
        console.log(client)
        var images = !{JSON.stringify(images)}
        var scene = document.getElementById('imageWall');
        var parallax = new Parallax(scene);
    script(src="/js/index.js")




